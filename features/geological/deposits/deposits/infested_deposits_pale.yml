id: INFESTED_DEPOSITS_PALE
type: FEATURE

anchors:
  - &structure INFESTED_DEPOSIT
  - &densityThreshold 1/256 * ${features/geological/deposits/distribution.yml:infested-pale.averageCountPerChunk} # Divide by 16^2 to get % per column
  - &salt $features/geological/deposits/distribution.yml:infested-pale.salt
  - &range $features/geological/deposits/distribution.yml:infested-pale.range

distributor:
  type: SAMPLER
  sampler:
    type: POSITIVE_WHITE_NOISE
    salt: *salt
  threshold: *densityThreshold

locator:
  type: RANDOM
  amount: 1
  height: *range
  salt: *salt

structures:
  distribution:
    type: CONSTANT
  structures: *structure