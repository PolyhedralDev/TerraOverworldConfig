id: MOUNTAIN_RIVER_DRIPSTONE_DEPOSITS
type: FEATURE

anchors:
  - &structure DRIPSTONE_DEPOSIT
  - &densityThreshold 1/256 * ${features/deposits/distribution.yml:river-dripstone.averageCountPerChunk} # Divide by 16^2 to get % per column
  - &salt $features/deposits/distribution.yml:river-dripstone.salt
  - &range $features/deposits/distribution.yml:river-dripstone.range

distributor:
  type: SAMPLER
  sampler:
    type: POSITIVE_WHITE_NOISE
    salt: *salt
  threshold: *densityThreshold

locator:
  type: RANDOM
  amount: 1
  height: *range
  salt: *salt

structures:
  distribution:
    type: CONSTANT
  structures: *structure