id: EQ_TROPICAL_ROCKY_ISLANDS # Valantis based equation 
type: BIOME
abstract: true

noise:
  dimensions: 3
  type: EXPRESSION
  expression: |
    if(
    y < 51,
    if(
      y < 64,
      if(
        y < 84,
        if(
          y < 256,
          0,
          -((y / 33)^2) + 4/6 + noise(x, z)*11),
        -((y / 33)^2) + 4/6 + noise(x, z)*12),
      -((y / 63)^3) + 1 + noise(x, z)/3),
    1)
  samplers:
    noise:
      type: FBM
      dimensions: 2
      octaves: 4
      function:
        type: OPEN_SIMPLEX_2
        frequency: 0.0075