id: EQ_HILLS
type: BIOME
abstract: true

# Basic hilly terrain.

vars: &variables
  base: 64
  height: 10

noise:
  type: EXPRESSION
  dimensions: 3
  variables: *variables
  expression: -y + base + (simplex(x, z)+1)/2 * height