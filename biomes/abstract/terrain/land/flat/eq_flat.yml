id: EQ_FLAT
type: BIOME
abstract: true
extends: NO_ELEVATION

# Perfectly flat terrain.

vars: &variables
  base: 64

noise:
  type: EXPRESSION
  dimensions: 3
  variables: *variables
  expression: -y + base + (simplex(x, z)+1)/6