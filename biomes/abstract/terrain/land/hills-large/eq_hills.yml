id: EQ_HILLS
type: BIOME
abstract: true
extends: NO_ELEVATION

# Basic hilly terrain.

vars: &variables
  base: 70
  height: 35

noise:
  type: EXPRESSION
  dimensions: 3
  variables: *variables
  expression: -y + base + (simplex(x, z)+1)/2 * height