num height = randomInt(4) + 1;
for (num i = 0; i < height; i = i + 1) {
    block(0, i, 0, "minecraft:cactus", false);
}

if (randomInt(3) == 0) {

    block(0, height, 0, "minecraft:cactus_flower");
}